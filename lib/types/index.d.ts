/**
 * Solarized-dsh-theme host half types: a no-op loader entry — the feature lives in
 * the browser half.
 */
/** Host loader entry for the browser implementation exported from `./client`. */
export declare function apply(): void;
